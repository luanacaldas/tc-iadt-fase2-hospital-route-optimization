# 📦 Instalação do Flask - Guia Rápido

## ✅ Flask Instalado!

Flask e Flask-CORS foram instalados com sucesso!

## 🚀 Como Usar

Agora você pode executar a interface completa do chatbot:

```bash
python run_chatbot_interface.py
```

## 💡 Dica: Comandos pip no Windows

No Windows/PowerShell, use sempre:

```bash
python -m pip install <pacote>
```

Ao invés de apenas:

```bash
pip install <pacote>
```

Isso garante que o pip correto seja usado.

## 📋 Pacotes Instalados

- ✅ `flask` - Framework web
- ✅ `flask-cors` - Suporte a CORS para API

## 🎯 Próximos Passos

1. Execute: `python run_chatbot_interface.py`
2. A interface abrirá no navegador
3. O servidor backend será iniciado automaticamente
4. O chatbot usará Ollama para respostas reais!

---

**Tudo pronto! 🎉**
